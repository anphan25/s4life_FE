name: CI/CD

on:
  push:
    branches: [ Configure/NamTH/CICD ]
  pull_request:
    branches: [ Configure/NamTH/CICD ]

jobs:
  # Build-Test:

  #   runs-on: ubuntu-latest

  #   strategy:
  #     matrix:
  #       node-version: [16.x]

  #   steps:
  #   - name: Checkout repository
  #     uses: actions/checkout@v2

  #   - name: Set up Node.js ${{ matrix.node-version }}
  #     uses: actions/setup-node@v1
  #     with:
  #       node-version: ${{ matrix.node-version }}

  #   - name: Install dependencies
  #     run: npm install

  #   - name: Build
  #     run: npm run build --if-present
  #     env:
  #        CI: ""

  Transfer-Code-To-Server-And-Deploy:
    runs-on: ubuntu-latest
    # needs: [Build-Test]
    if: github.ref == 'refs/heads/Configure/NamTH/CICD'
    steps:
    - uses: actions/checkout@master
    - name: Get The source code
      uses: appleboy/scp-action@master
      with:
        host: 139.59.223.66
        username: tester
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: 22
        source: "."
        target: "s4life_FE"

    - name: Deploying
      uses: appleboy/ssh-action@master
      with:
        host: 139.59.223.66
        username: tester
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: 22
        script: |
          cd s4life_FE
          npm run build
